# ADR 2026-05-26: IG Comments прицепляются к открытой сущности B24

## Контекст

До этого ADR действовало правило A2: «один (клиент × пост) = одна сессия
= один лид». При комментировании клиентом 5 разных постов создавалось
5 разных лидов, связанных с одним контактом через `UF_CRM_IG_CHAT_ID`.

Дмитрий 2026-05-26 пересмотрел правило: лишних лидов плодить не нужно.
Если у клиента уже есть открытая сделка или лид — все комменты должны
идти туда.

## Новое правило

**Приоритет — открытая сущность B24 у клиента.** Иерархия:

1. У клиента есть **открытая сделка** → коммент идёт в **эту сделку**
2. Открытой сделки нет, но есть **открытый лид** → коммент идёт в **этот лид**
3. Ни открытой сделки, ни лида → создаётся **новый лид**

Применяется:
- К **любому каналу IG** (Direct и Comments) — не зависит от линии
- К **любому посту** — новый пост ≠ новый лид
- По **контакту в B24** (по `UF_CRM_IG_CHAT_ID` или phone) — не по chat.id

То же поведение для **outgoing IG Direct из существующего лида/сделки**:
не создавать новый лид, открыть линию в рамках текущей сущности.

## Что считать «открытым»

**Лид** — STATUS_ID не в {`CONVERTED`, `JUNK`} (любой `NEW`/`IN_PROGRESS`/
`PROCESSED`/etc. = открыт).

**Сделка** — `STAGE_ID` не в финальных стадиях (`WON`, `LOSE`). Стадии
«в работе»/«ожидает оплаты»/etc. = открыта.

Точный список финальных стадий — в B24 настройках портала, нужно
зафиксировать при реализации.

## Реализация (TODO)

Это **изменение архитектуры** flow adapter. Сейчас `handleI2crmIncoming`
сразу вызывает `imconnector.send.messages`, что создаёт сессию + лид
если нет существующего chat.id. Новая логика требует **перехвата перед
imconnector**.

### Псевдокод

```ts
async handleI2crmIncoming(payload) {
  // 1. Валидация (как сейчас)
  // 2. Найти открытую сущность у клиента:
  //    const openDeal = await findOpenDealByIgChatId(clientId);
  //    const openLead = openDeal ? null : await findOpenLeadByIgChatId(clientId);
  //    const target = openDeal ?? openLead ?? null;
  //
  // 3. Если target:
  //    - НЕ вызывать imconnector.send.messages
  //    - crm.timeline.comment.add(target, text + ссылка на пост IG)
  //    - mirror в TG/customer-360 как обычно
  //    - return success
  //
  // 4. Иначе (нет открытой сущности):
  //    - imconnector.send.messages с chat.id = i2crm_ig_<client>
  //      (БЕЗ media_id-суффикса — все посты в одну сессию)
  //    - создаст новый лид через open line CRM_CREATE=lead
}
```

### Изменение chat.id

Старый формат: `i2crm_ig_<client>_c<media>` (A2)
Новый формат: `i2crm_ig_<client>` (одинаковый для всех постов и Direct)

Этап миграции:
- Текущие сессии с A2 chat.id остаются (B24 их не удалит)
- Новые комменты — с новым chat.id
- Постепенно старые сессии закроются (24ч без активности)

### Где найти открытую сущность

`crm.lead.list` с фильтром:
```
filter: {
  UF_CRM_IG_CHAT_ID: <clientId>,
  "!STATUS_SEMANTIC_ID": ["F", "S"]  // F=finished, S=successful — закрытые
}
```

Аналогично `crm.deal.list` с фильтром по `STAGE_SEMANTIC_ID` (открытые
сделки: P=process).

## Открытые вопросы (нужны ответы Дмитрия)

1. **Когда коммент идёт в timeline существующей сделки** — оператор видит
   его **только в карточке сделки** (раздел «Лента»). В Open Line чате
   ничего нет. Если оператор хочет ответить на коммент — как?
   - Вариант A: всё ОК — оператор отвечает через виджет «Social Connector»
     в карточке сделки. Виджет должен поддерживать отправку коммента
     обратно в IG.
   - Вариант B: параллельно создавать сообщение в Open Line чате (но
     ассоциированном с этой сделкой). Сложно.
   - Вариант C: добавлять кнопку «Ответить в IG» прямо в timeline-комменте.

2. **Reply на конкретный коммент клиента** — если у нас в timeline
   сделки 5 комментов от клиента, и оператор хочет ответить на 3-й.
   Сейчас reply работает в Open Line (через quote), а в timeline нет
   native quote-механизма.

3. **Comments линия 22 — что с ней?** Если все комменты уходят в
   существующую сущность как timeline — линия 22 практически не нужна.
   Только для холодных случаев (новый клиент, никаких сделок/лидов нет).

4. **Что если в момент коммента у клиента ДВЕ открытых сделки** (редкий,
   но возможный случай)? В какую идёт коммент? По дате создания (новейшая)?

## Влияние на другие документы

- `INSTAGRAM_FLOW.md` — обновить раздел A2, добавить новый flow
- `OPEN_LINE_LIFECYCLE.md` — обновить разделы про создание сессии
- `SEQUENCES.md` — обновить sequence для IG Direct + Comment
- `bitrix24.service.ts:handleI2crmIncoming` — переписать flow
- `bitrix24.service.ts:handleI2crmOutgoing` — учесть что коммент мог
  прийти в timeline, не в Open Line
- Тесты `src/common/i2crm-payload.spec.ts` — обновить ожидания chat.id

## Статус

- [x] Дмитрий зафиксировал правило (2026-05-26)
- [x] PRODUCT_RULES.md обновлён
- [x] ADR создан (этот документ)
- [ ] Открытые вопросы (см. выше) — ответы Дмитрия нужны
- [ ] Реализация в `handleI2crmIncoming` + новый chat.id формат
- [ ] Обновление документации (INSTAGRAM_FLOW, SEQUENCES, OPEN_LINE_LIFECYCLE)
- [ ] Обновление тестов
- [ ] Backfill / migration cleanup старых A2 сессий (опционально)

## Связано

- Предыдущий ADR (был неявный) — формат A2 chat.id
  `i2crm_ig_<client>_c<media>`
- `decisions/2026-05-26-crm-create-second-disabled.md` — `CRM_CREATE_SECOND=N`
  для повторных обращений на уровне B24 (это про повторный chat.id, наш
  новый случай — это про другой chat.id, другой логический подход)
- `PRODUCT_RULES.md` §2.1
- `INSTAGRAM_FLOW.md` §6 (A2 формат), TODO update
